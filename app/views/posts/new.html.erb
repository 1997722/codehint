<div class="container background-section2">
  <h1 class="text-center">Let's post!</h1>
    <div class="row">
      <div class="col-10 offset-1 col-md-4 offset-md-4"> 
        <%= form_with(model: @post, url: posts_path, local: true) do |f| %>
          <div class="form-group">
            <%= f.text_area :content, class: "form-control posts-text", id: "post-info", placeholder: "コーディネートの説明を一言！", rows: "4", maxlength: "40" %>
          </div>
          <br>
          <div class="form-group">
            <%= f.text_field :tag_name, class: "form-control", placeholder: "タグ" %>
            <p><small>※半角スペースを空けることで、複数のタグを付けることが出来ます。 例：雨の日 30代</small></p>
          </div>
          <div class="form-group text-center">
            <%= f.file_field :image, id: "post-image", class: "form-control-file" %>
            <%= image_tag @post.image, class: 'img-thumbnail mx-auto d-block' if @post.image.attached? %>
            <br>
            <small class="text-muted">画像サイズ: 400px x 400px がおすすめです。</small>
          </div>
          <br>
          <div class="text-center">
            <%= f.submit "投稿する", class: "btn btn-primary post-btn" %>
            <%= link_to 'もどる', root_path, class: "btn btn-secondary back-btn" %>
          </div>
        <% end %>
      </div>
    </div>
</div>
